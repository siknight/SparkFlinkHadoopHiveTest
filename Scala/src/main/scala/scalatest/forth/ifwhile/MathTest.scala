package scalatest.forth.ifwhile
//目前我还没发现自动导包快捷键，需要手动导包
import scala.math._
import scala.xml.Null

object MathTest {
  def main(args: Array[String]): Unit = {
      //函数
      val d: Double = sqrt(100)
      println("d="+d)
      println("---------");
  }
}
